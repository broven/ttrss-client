{
  "name": "ttrss-client",
  "version": "0.0.1",
  "main": "index.js",
  "repository": "git@github.com:broven/ttrss-client.git",
  "author": "metajs <zycode277@gmail.com>",
  "scripts": {
    "tslint": "tslint -c tslint.json -p tsconfig.json",
    "test": "jest --coverage",
    "test:mock": "jest --config jest.mock.config.js --coverage",
    "test:ci": "jest --config jest.mock.config.js --ci",
    "test:cicov": "jest --config jest.mock.config.js --coverage --ci",
    "doc": "typedoc --out docs src/index.ts",
    "dev": "tsc -w & jest --config jest.mock.config.js --watch --coverage"
  },
  "dependencies": {
    "axios": "^0.18.0"
  },
  "devDependencies": {
    "@types/axios": "^0.14.0",
    "@types/dotenv": "^6.1.0",
    "@types/jest": "^24.0.11",
    "codecov": "^3.2.0",
    "dotenv": "^7.0.0",
    "jest": "^24.5.0",
    "ts-jest": "^24.0.2",
    "tslint": "^5.14.0",
    "typedoc": "^0.14.2",
    "typescript": "^3.4.5"
  }
}
